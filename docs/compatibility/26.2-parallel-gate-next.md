# 26.2 parallel gate next

## Purpose and boundary

This gate collects the four preceding parallel handoffs and runs their requested
checks in the shared worktree. It is limited to compile/test corrections needed
to make the handoffs executable plus this evidence document. No generated data,
configuration, server trigger, gameplay policy, or unrelated specification was
added. `git stage` and `git commit` were not run.

The task-owned Rust target was isolated at `C:/Temp/pumpkin-target-26_2-next`.
The Rust commands used `RUSTUP_TOOLCHAIN=1.97.1`,
`CARGO_INCREMENTAL=0`, `CARGO_PROFILE_DEV_DEBUG=0`,
`CARGO_BUILD_JOBS=2`, and `--locked`. All logs are outside the repository under
`C:/Temp/pumpkin-26_2-gate-logs/`.

## Findings

### A: real World entity persistence E2E

- Symbol: `world::tests::world_entity_persistence_e2e_spawn_move_stop_reopen`
  in `crates/pumpkin/src/world/mod.rs`.
- Scope exercised: concrete armor-stand spawn through the production World path,
  live position/custom-NBT mutation, chunk-boundary move, new spawn, `World::save`,
  `World::shutdown`, independent `World`/`Level` reopen, exact UUID/type/Pos/
  `PumpkinCustomData`, old-chunk emptiness, and duplicate-UUID rejection.
- First command exit `101` was environmental: the isolated target contained
  generated `include_str!` paths from the prior detached worktree
  `C:/Temp/pumpkin-26_2-e2e-verify`. No repository source was changed for this.
  The external target was removed (`11,094,691,141` bytes) and rebuilt.
- Final command: `cargo test --locked --lib -p pumpkin
  world::tests::world_entity_persistence_e2e_spawn_move_stop_reopen -- --nocapture`.
  Exit `0`; `1 passed`, `0 failed`, `281 filtered`.
- Lower-level persistence coverage was also included in the later full package
  run: the three `pumpkin-world` entity persistence/error tests and the World E2E
  all passed as part of the package's `282` tests.

### B: entity attack/interact authority

- Symbols: `valid_entity_interaction`,
  `is_within_attack_range`, `is_within_entity_interaction_range`,
  `JavaClient::handle_attack`, and `JavaClient::handle_interact` in
  `crates/pumpkin/src/net/java/play/attack.rs` and `interact.rs`.
- Final command: `cargo test --locked --lib -p pumpkin
  net::java::play::attack::tests -- --nocapture`.
  Exit `0`; `5 passed`, `0 failed`, `277 filtered`.
- Covered boundaries include target-box distance, inclusive attack reach,
  strict interaction reach, creative/attribute overrides, non-finite inputs,
  removed/dead/spectator/different-world rejection. The package compile was
  completed by this test and by the final `cargo check` below.
- No LOS guard was added: the preceding specification handoff records that the
  official 26.2 packet handler does not require a block raycast for these paths.

### C: secure-chat state and known answers

- Symbols: `verify_and_commit`, `preview_last_seen`,
  `last_seen_checksum`, and the canonical RSA tests in
  `crates/pumpkin/src/net/chat/state.rs` and `signature.rs`; production wiring is
  in `crates/pumpkin/src/net/java/play/chat_message.rs`.
- Final command: `cargo test --locked --lib -p pumpkin net::chat:: -- --nocapture`.
  Exit `0`; `16 passed`, `0 failed`, `266 filtered`.
- The final run includes the canonical empty-last-seen known answer, independent
  RSA verification and mutation rejects, Java signed-byte checksum (`0xe1`),
  inbound index/timestamp state, ACK offset/20-bit preview, and atomic reject
  preservation.
- The only Rust test changes made during the gate were fixture/assertion fixes:
  the ACK fixture now models the validator's bounded window explicitly (including
  the offset case's trailing empty slots and a separate unknown-slot fixture),
  and compares `matches!(...)` rather than requiring `LastSeenMessagesValidator`
  to implement `PartialEq`. Production chat behavior was not relaxed to satisfy
  a failing assertion.

### D: Python harness and compatibility data

- Command: `py -m unittest discover -s tools/compat-26_2 -p 'test_*.py' -v`.
  Exit `0`; `23 passed`, `0 failed`.
- Command: `py -m py_compile tools/compat-26_2/*.py`. Exit `0`; `3` Python files
  compiled.
- Read-only compatibility-data parse/shape check: `5` JSON fixtures,
  `27` case entries, `28` decision entries; JSON/schema/evidence/expected fields
  parsed successfully. Distribution was `accept=9`, `reject=14`, `defer=2`,
  plus three policy-specific strings:
  `accept_as_unsigned_system_or_disguised_chat`,
  `accept_as_unsigned_policy_path`, and `accept_as_unsigned_command`.
  Those three values are retained as a documented residual because the fixture
  README's simplified `accept|reject|defer` wording does not cover the existing
  policy fixture vocabulary. No fixture file was edited and no semantic vanilla
  acceptance claim is made.
- No socket/server run was performed. The reconfiguration harness remains
  prepared-only: no production `start_reconfiguration` caller has been run.

## Compile and regression gates

- `cargo check --locked -p pumpkin`: exit `0`.
- `cargo test --locked --lib -p pumpkin -- --nocapture`: exit `0`;
  `282 passed`, `0 failed`, `0 ignored`.
- `cargo fmt --all -- --check`: exit `0` after formatting the narrow state-test
  fixture correction.
- `git diff --check`: exit `0`; Git emitted only existing LF-to-CRLF conversion
  warnings for several modified files.
- The known `proc-macro-error2 v2.0.1` future-incompatibility notice remains;
  it did not affect exit status.

## Implementation range and current artifact state

The shared worktree changes remain limited to the preceding handoffs plus the
following compile/test cleanup:

- Persistence: `crates/pumpkin-world/src/level.rs` and
  `crates/pumpkin/src/world/mod.rs`.
- Entity authority: `attack.rs`, `interact.rs`, and removal of stale imports in
  `crates/pumpkin/src/net/java/play/mod.rs`.
- Chat state: `net/chat/state.rs`, `net/chat/mod.rs`, and
  `net/java/play/chat_message.rs`.
- TCP harness: `tools/compat-26_2/protocol776_harness.py` and its unit tests.
- Documentation handoffs already present in `docs/compatibility/`; this file is
  the gate summary. The separate review document
  `docs/compatibility/26.2-parallel-independent-review.md` is read-only here.

A fresh test executable exists outside the repository at
`C:/Temp/pumpkin-target-26_2-next/debug/deps/pumpkin-a6b6aa0962ea9581.exe`
(81,166,336 bytes at the final inspection). No standalone server binary exists
at `C:/Temp/pumpkin-target-26_2-next/debug/pumpkin.exe`; no server was started.
The repository `world/`, user data, `.pi/`, logs, and JARs were not modified by
this gate.

## Review requests and residuals

1. Review the persistence owner boundary around `World::save`/
   `World::shutdown`, `World::save_entity`, and `Level::save_entity_nbt`, with
   special attention to removal of a moved UUID from the old cached entity
   chunk.
2. Review the attack/interact authority order before plugin events, damage, and
   item interaction; confirm same-world identity, target lifecycle checks,
   attackable-target semantics, inclusive attack versus strict interaction
   boundaries, and the intentional absence of LOS enforcement.
3. Review the chat sidecar lifecycle and session replacement behavior. In
   particular, outbound `broadcast_secure_player_chat` still has its pre-existing
   outbound/nil-session behavior and sidecar disconnect cleanup remains a
   follow-up outside this gate.
4. Review the Python reconfiguration negative-case classifications and the
   compatibility-data vocabulary residual above. Synthetic harness tests are not
   vanilla-client or server-trigger evidence.
5. No server-triggered TCP reconfiguration, vanilla client connection, server
   startup, or release binary build is claimed. These remain the next gate after
   an owner-approved production/test seam and independent review.

# 26.2 再開状況回収と次の統合 gate 計画

- 対象: Java Edition `26.2` / protocol `776` / world data version `4903`
- 回収日: 再開指示時点
- この回で変更したファイル: **この文書のみ**
- 今回の禁止事項の遵守: `cargo` / `rustc` / build / server起動 / commit / reset / clean / amend / rebase / push は未実行。`.pi/`、goal、world、log、JAR、raw data、ユーザーデータは未変更。
- 判定: E は実 TCP の二周期＋Configuration中EOFの test 証拠あり。F/G は source と handoff doc から変更内容を回収したが、この回の Rust test は未実行。正式 vanilla 26.2 client と完全互換は未検証。

## Findings

### 1. Git / worktree の live 状態

確認コマンドと結果:

```text
git status --short --branch --untracked-files=all
```

- 終了コード `0`。
- `master...origin/master [ahead 6]`。`HEAD=4558265553c15c02b62921629859f44c898476b2`、`origin/master=d641bd44a`。
- checkpoint 文書にある「ahead ten」などの過去記録より、今回の live Git 出力を優先する。
- staged path は `0`。現在の tracked modified は次の5件:
  - `crates/pumpkin/src/net/chat/state.rs`
  - `crates/pumpkin/src/net/java/play/attack.rs`
  - `crates/pumpkin/src/net/java/play/interact.rs`
  - `crates/pumpkin/src/net/java/play/mod.rs`
  - `crates/pumpkin/src/world/mod.rs`
- untracked は E/F/G の test/doc と `.pi/goals/` の既存保護ファイルを含む。`.pi/goals/.goals-pool-snapshot.json` とその `.tmp` は未追跡のまま、編集・stage対象外。
- `git diff --stat`: **608 insertions / 69 deletions**。`git diff --cached --stat`: 出力なし。
- `git diff --check`: 終了コード `0`。tracked Rust の LF→CRLF 警告のみで whitespace error はない。
- 直近 log は `455826555 docs(compatibility): include final persistence format checkpoint`、その下に `56943b3af`、`bf704ab82`、`1c7e5a47d`、`57ab802ca`、`44b5b3319`。この回は履歴を変更していない。

`rust-toolchain.toml` は `channel = "stable"` であるため、この回は toolchain を暗黙に信頼せず、次の実行 gate では明示的に `RUSTUP_TOOLCHAIN=1.97.1` を付ける必要がある。今回は `rustc` / `cargo` 自体を実行していない。

### 2. process / port / disk 確認

他所有 process を止めない条件で read-only 確認を行った。

- `ps -ef | grep -Ei 'cargo|rustc|pumpkin|java|proxy|agent|python'`: 該当行なし。
- `cmd.exe /c "tasklist /v /fo csv"` の同名絞り込み: 該当 process 行なし（shell prompt のみ）。
- `netstat -ano`: `25565`、`2557x`、`2558x` 等の Pumpkin test/server port の LISTENING はなし。
- 既存の別用途と見られる port は残存している。例: `127.0.0.1:1337` PID `5892`、`2222` PID `36572`、`2223` PID `28868`、`2225` PID `32444`、`13331` PID `7924`、`13344` PID `3168`、`14834` PID `38216`、`29872` PID `35636`、`40158/54571` PID `24772`。これらは所有確認なしに停止していない。
- C: free は `183,546,277,888` bytes（約 `170.8 GiB`）。
- 専用 target `C:/Temp/pumpkin-target-26_2-next`: `6,836,489,307` bytes（`du -sb`）。
- repo `C:/Users/yuzum/Desktop/mine_rust_server/Pumpkin/target`: `19,984,778,687` bytes。重複 target は削除していない。
- `find /c/Users/yuzum/Desktop /c/Temp -maxdepth 5 -type d -name .git` で Pumpkin 系に該当した repository は現 cwd の `Pumpkin/.git` だけ。別 repository を触っていない。

process のコマンドライン取得はこの環境の `ps`/Windows wrapper 出力に依存するため、PIDの所有者を完全同定した証拠ではない。ただし今回の停止操作は `0` 件である。

### 3. E: 実 TCP reconfiguration evidence の回収

#### 変更 / symbols / owner

Owner は **E / reconfiguration integration**。

- `crates/pumpkin/tests/java_reconfiguration_tcp.rs`
  - integration test: `java_reconfiguration_over_real_tcp_preserves_player_state`
  - helpers: `spawn_harness`、`wait_for_player`、`wait_for_state`、`wait_for_play_baseline`、`inventory_snapshot`、`run_listener`
  - real `PumpkinServer::new`、real `PumpkinServer::unified_listener_task`、OS assigned loopback port、server-owned `Arc<Player>`、`JavaClient::start_reconfiguration()`、real shutdown pathを使用。
- `crates/pumpkin/tests/protocol776_reconfiguration_client.py`
  - test-only adapter。既存 `tools/compat-26_2/protocol776_harness.py` と `assets/packet/26_2_packets.json` を使用し、Start Configuration を合成しない。
- `docs/compatibility/26.2-reconfiguration-tcp-evidence.md`
  - synthetic peer と正式 client の証拠境界、normal/EOF結果、未検証項目を記録。

#### 回収できた実施結果

既存 evidence doc 記載の focused command:

```text
RUSTUP_TOOLCHAIN=1.97.1 \
CARGO_TARGET_DIR=C:/Temp/pumpkin-target-26_2-next \
CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 CARGO_BUILD_JOBS=2 \
cargo test --locked -p pumpkin --test java_reconfiguration_tcp -- --nocapture
```

- 終了コード `0`、`1 passed / 0 failed / 0 ignored`、経過 `21.90 s`。
- test binary は `C:/Temp/pumpkin-target-26_2-next/debug/deps/java_reconfiguration_tcp-598bf4d75635e09e.exe`。
- `proc-macro-error2 v2.0.1` の既知 future-incompatibility notice 以外に失敗なし。
- normal connection は同一 TCP connection で二周期を完了:
  - `start_configuration`: clientbound `2`
  - 空 `configuration_acknowledged`: serverbound `2`
  - `finish_configuration`: clientbound `2`
  - 空 Finish ack: serverbound `2`
  - duplicate Finish ack: `0`
  - `join_game`: `1`（再joinなし）
  - harness result: `stateful_reconfiguration_pass`、`cycles_completed=2`
  - `same_entity=true`、`same_position=true`
- Rust側では二周期を通じて entity ID、position、同じ `Arc<Player>`、同じ `Arc<World>`、main inventory 36 slot の `(item id, count)` snapshot を比較。
- 直後の independent EOF probe は `disconnect_during_config`。harness exit `0`、result `client_disconnected_during_configuration`、server disconnect `client_eof`、接続状態は `Play` に戻らない。
- trigger は test から `JavaClient::start_reconfiguration()` を二回呼ぶだけで、production caller、command、RCON、stdin、hidden hookはない。

#### E が証明したこと / 証明していないこと

証明したのは、**現 source の実 Pumpkin server/listener と server-owned Player/JavaClient が、synthetic protocol peerとの bounded な Play→Configuration→Play を同一接続で二回受理し、server-side ownership/stateを維持したこと**である。EOF時に invalid な Play recovery を観測しなかったことも証明した。

次は証明していない:

- 正式 Java 26.2 vanilla client の decode/accept/render。
- Registry Data、Tags、chunk section、NBT、resource-packの意味論・適用。
- online-mode、encryption、gameplay、inventory UI。
- production runtime からの reconfiguration trigger。
- standalone server process の graceful exit code。testは in-process `Server::shutdown()` の `Result` を確認しただけで、別 executable の終了コードではない。

### 4. F: attack / interact item guards の回収

#### 変更 / symbols / owner

Owner は **F / entity interaction authority**。

- `crates/pumpkin/src/net/java/play/attack.rs`
  - `valid_entity_target_state`: same-world、removed、attack時 spectator/dead living を side effect 前に拒否。
  - `squared_distance_to_box`、`is_within_attack_range`、`is_within_entity_interaction_range`: finite値、square overflow、attack outer inclusive、entity interaction outer strict を検査。
  - `entity_attack_range`: `AttackRangeImpl` または `ENTITY_INTERACTION_RANGE`、creative bonus を使用。
  - `can_attack_with_item` / `attack_strength_meets_charge`: `MinimumAttackChargeImpl` と既存 `last_attacked_ticks` / item attack speedから、公式 `cannotAttackWithItem(item, 5)` 相当を read-onlyで判定。
  - `can_use_ordinary_attack_item`: 通常 `Player::attack` では `PiercingWeaponImpl` を拒否。
  - `JavaClient::handle_attack`: target authority → item guard → creative protection → `Player::attack` の順。
- `crates/pumpkin/src/net/java/play/interact.rs`
  - attack/interact 共通 target guard を event、damage、item mutation より前に適用。
  - packet hand `0/1` を `Hand::Right/Left` に検証し、選択された handだけで event/use/stat/break status/inventory mutationを行う。
  - `InteractAt` eventも item mutation 前に処理。
  - PiercingWeaponを entity interact 全体に blanket rejectしていない。
  - item required-feature metadataが現 Pumpkin `Item` modelにないため、feature enabled判定は意図的に未完。
- `crates/pumpkin/src/net/java/play/mod.rs`
  - stale importの除去のみ。新しい public exportなし。
- `docs/compatibility/26.2-interaction-item-guards.md`
  - 公式 `javap` 根拠、現 modelとの差、resolvedとmust-fixを記録。

公式調査として、`C:/Temp/server-26.2.jar` および保存済み `javap` 出力をread-only照合している。attackは world border/AABB/attackable/item enabled/minimum charge/PiercingWeapon の順、interactは border/AABB/selected-hand item enabled/interactOn の順であることを確認した。packet handlerにblock LOS raycastがない点も維持した。

#### 実施済み確認と未実施

F handoffに記載された確認:

- `rustfmt --edition 2024 --check` 対象 attack/interact: 終了コード `0`。
- `git diff --check` 対象F: 終了コード `0`、LF→CRLF warningのみ。
- `cargo test` / `cargo check` / `clippy` / build / server / vanilla client: 指示により未実行。

source内の現在の pure test 名は6件:

1. `attack_range_uses_inclusive_outer_boundary_and_target_box_distance`
2. `interaction_range_uses_strict_outer_boundary`
3. `creative_and_attribute_overrides_change_the_authoritative_range`
4. `non_finite_range_inputs_are_rejected`
5. `minimum_attack_charge_is_a_pre_attack_guard`
6. `removed_and_dead_targets_are_rejected_before_side_effects`

残must-fix / 未検証:

- generated `Item` に `required_features` 相当がなく、item名やcomponent presenceから experimental featureを推測拒否していない。これは未解消であり、vanilla標準 itemとの混同を避けるための正しい保留。
- 実 `Player` fixtureで minimum charge `0/1`、cooldown不足/到達、拒否時の event/damage/stat/item mutation `0` を未検証。
- interactの main/off-hand、`InteractAt`、creative、PiercingWeapon、dead/spectator target、border外、dragon part の実 packet pathを未検証。
- item feature gate、signed command、outbound secure attribution、vanilla client runtimeはこの変更の完了条件ではない。

### 5. G: chat sidecar lifecycle cleanup の回収

#### 変更 / symbols / owner

Owner は **G / chat lifecycle**。

- `crates/pumpkin/src/net/chat/state.rs`
  - `InboundChatState.owner_generation` を追加。
  - `reset_inbound_state`: 同一 session UUIDの再通知をno-op、新 sessionでchainを交換。
  - `clear_inbound_state(player_id, session_id, owner_generation)`: session UUIDとPlayer allocation generationの一致時だけ削除。
  - `verify_and_commit`: 成功時だけ index/timestamp/ACK validator と owner generation をcommit。
  - 既存6件に加え lifecycle test 4件を追加し、現在の state test数は10件。
- `crates/pumpkin/src/world/mod.rs::World::remove_player`
  - UUIDだけでなく `Arc::ptr_eq` で正確な Player ownerをremove。
  - `fire_event=true`（disconnect）のみ sidecar cleanup。
  - `fire_event=false`（dimension transfer）はsidecarを保持。
  - 他worldを含む同UUID replacementを検出した場合、old connectionは新 stateを消さない。
  - matching `(profile UUID, session UUID, Arc<Player> generation)` を `clear_inbound_state` に渡す。
- `docs/compatibility/26.2-chat-lifecycle.md`
  - disconnect/transfer/duplicate-login raceの境界を記録。

#### 現在の10 state test名

1. `root_and_next_indices_advance_only_after_success`
2. `old_timestamp_is_separate_and_does_not_advance`
3. `chain_break_is_sticky_after_replay_or_signature_failure`
4. `checksum_uses_java_signed_bytes_and_order`
5. `nonempty_last_seen_matches_fixture_hash`
6. `ack_preview_preserves_order_and_original_on_rejection`
7. `same_session_reset_is_idempotent`
8. `old_disconnect_cannot_clear_new_session_or_duplicate_login_state`
9. `old_player_generation_cannot_clear_same_session_replacement`
10. `broken_chain_is_removed_by_matching_disconnect_cleanup`

#### 実施済み確認と未実施

G handoffに記載された確認:

- `git status --short --branch`: 終了コード `0`、既存変更と`.pi/`を保持。
- callsite audit: disconnectは`true`、dimension transferは`false`。
- `cargo` / `rustup` / server / vanilla client: 指示により未実行。

したがって現在は source/doc回収であり、Gの compile/test pass はこの回の証拠として追加しない。`clear_inbound_state` は broken chainもmatching disconnectでは消すが、signed command verification、outbound secure attribution、実 runtime duplicate-login ordering、official runtime chat traceは未完了である。

## Relevant Files

### 今回のE/F/G責任境界

| Owner | 変更対象 | 次の主gate |
|---|---|---|
| E | `crates/pumpkin/tests/java_reconfiguration_tcp.rs`、`crates/pumpkin/tests/protocol776_reconfiguration_client.py`、`docs/compatibility/26.2-reconfiguration-tcp-evidence.md` | real TCP test binary / synthetic peer boundary |
| F | `crates/pumpkin/src/net/java/play/attack.rs`、`interact.rs`、`play/mod.rs`、`docs/compatibility/26.2-interaction-item-guards.md` | attack pure tests、次に実 Player/packet fixture |
| G | `crates/pumpkin/src/net/chat/state.rs`、`crates/pumpkin/src/world/mod.rs`、`docs/compatibility/26.2-chat-lifecycle.md` | state tests、signature regression、caller compile |

`docs/compatibility/26.2-entity-interaction-authority.md` は status上 untracked だが、今回指定された Fの `interaction-item-guards` とは別の既存 handoff docとして扱い、owner確認なしにcommitへ含めない。`.pi/` と goal snapshotも同様にstage禁止。

### 競合

- E/F/Gの現在指定pathは相互に直接重複しない。ただしF/G/Eはいずれも `pumpkin` packageの同一 compile graphを使うため、次の test実行では共有 target `C:/Temp/pumpkin-target-26_2-next` と Cargo lockを競合させない。Cargo test/checkは同時実行せず、調査・diff reviewだけを最大3並列にする。
- `World::remove_player` の所有はG。Fは `play/mod.rs` のみであり、`world/mod.rs` を修正しない。
- Eの testは現在の全Java/world/chat sourceを実際にcompileするため、F/Gのsource review後、少なくとも `cargo check --locked -p pumpkin` 成功後に実行する。
- local commit前に他ownerの差分が混入していないことを `git diff --name-only` と対象path限定diffで確認する。ユーザー変更をreset/cleanで除去しない。

## Migration / Investigation Notes

### 次担当が使う共通環境

Cargoを実行できる次の gateでは、全コマンドに以下を付ける:

```text
RUSTUP_TOOLCHAIN=1.97.1
CARGO_TARGET_DIR=C:/Temp/pumpkin-target-26_2-next
CARGO_INCREMENTAL=0
CARGO_PROFILE_DEV_DEBUG=0
CARGO_BUILD_JOBS=2
--locked
```

今回の制約下では上記を使った Cargo command は実行していない。既存 evidenceに書かれた pass数は「回収した過去の結果」であり、今回の再実行結果ではない。

### 0 test --exact 罠を避ける実行コマンド

`cargo test ... --exact` は、module filterだけを渡すと test binary側で一致0件になり得る。まず module/path filterを**exactなし**で使う。individual exactが必要な場合だけ、完全修飾 test名を渡して `-- --exact` を付ける。

#### E

```text
RUSTUP_TOOLCHAIN=1.97.1 CARGO_TARGET_DIR=C:/Temp/pumpkin-target-26_2-next CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 CARGO_BUILD_JOBS=2 cargo test --locked -p pumpkin --test java_reconfiguration_tcp -- --nocapture
```

対象は `java_reconfiguration_over_real_tcp_preserves_player_state` の1件。必要なら次の完全名で一件指定する:

```text
cargo test --locked -p pumpkin --test java_reconfiguration_tcp java_reconfiguration_over_real_tcp_preserves_player_state -- --exact --nocapture
```

#### F

最初の safe command:

```text
cargo test --locked --lib -p pumpkin net::java::play::attack::tests -- --nocapture
```

これは現在の6件をまとめて実行する。個別 exactを使う場合の完全名は、例えば:

```text
cargo test --locked --lib -p pumpkin net::java::play::attack::tests::minimum_attack_charge_is_a_pre_attack_guard -- --exact --nocapture
```

同じ形式で上記6 test名を指定する。interactに独立 test moduleは現在ないため、`net::java::play::interact::tests` を実行済みと報告してはいけない。

#### G

最初の safe command:

```text
cargo test --locked --lib -p pumpkin net::chat::state::tests -- --nocapture
cargo test --locked --lib -p pumpkin net::chat::signature::tests -- --nocapture
```

stateは現在10件を対象にする。signatureは既存の canonical/RSA regressionを別 gateとして確認する。完全名 exactでは、例えば:

```text
cargo test --locked --lib -p pumpkin net::chat::state::tests::old_disconnect_cannot_clear_new_session_or_duplicate_login_state -- --exact --nocapture
```

Gのproduction callerまで含む compile gate:

```text
cargo check --locked -p pumpkin
cargo fmt --all -- --check
```

### review → test → local commit の順序

1. 各 owner が対象path限定で source/doc diff、公式根拠、未検証項目を再確認する。
2. FとGのpure testsを順番に実行し、Eはその後に実 TCP integrationを実行する。共有 targetを同時に使わない。
3. E/F/Gそれぞれについて、対象pathだけを明示して `git add` する。`git diff --cached --stat`、`git diff --cached --check`、`git diff --cached --name-status`、cached full diffをレビューしてから local commitする。
4. 提案する分割:
   - E: `test(protocol): add 26.2 reconfiguration TCP evidence`
   - F: `fix(protocol): guard 26.2 entity interactions`
   - G: `fix(chat): clean 26.2 inbound state on player disconnect`
5. commitの前後で `.pi/`、goal、world/log/binary/JAR/raw data、他ownerの変更がstageされていないことを確認する。pushはしない。

### 最大3並列の次作業

次の最大3並列は、**cargo実行を同時にしない**前提で以下に分ける:

- **E**: TCP evidence docとtest adapterの再review、必要ならE専用 integration testを単独実行。
- **F**: attack pure 6件を実行し、実 Player fixtureが必要な未検証境界と required-feature modelの別責任化を整理。feature metadataを今回のcommitへ拡張しない。
- **G**: chat state 10件＋signature testsを実行し、`World::remove_player` caller/transfer pathをレビュー。chat auth/signed commandをこのgateの完了と扱わない。

3 ownerが同時に source editする場合も、path ownerを越えない。F/Gの間に直接path競合はないが、Eの integration testはpackage全体をcompileするため、各source editを止めてから実行する。

## Recommended Next Steps

1. まず live `git status` と対象path限定diffを再取得し、E/F/G以外のユーザー変更を保護したまま ownerごとに reviewする。
2. Fの `net::java::play::attack::tests` 6件、Gの `net::chat::state::tests` 10件と `net::chat::signature::tests` を、上記の `--exact` 罠を避けるコマンドで順番に実行する。失敗時はそのownerのpathだけを修正する。
3. `cargo check --locked -p pumpkin` と `cargo fmt --all -- --check` を共有 compile gateとして実行する。`RUSTUP_TOOLCHAIN=1.97.1` と専用targetを必ず明示する。
4. Eの `java_reconfiguration_over_real_tcp_preserves_player_state` を実行し、過去の **1 passed / normal 2 cycles / EOF negative** と同じ結果が再現するかを記録する。結果が再現しない場合、synthetic/vanillaの境界を保ったまま原因を分類する。
5. 各ownerが明示pathだけを local commitする。今回の回ではcommitしない。
6. 次の統合gateでも、正式 vanilla 26.2 client未検証、synthetic protocol peerの限界、opaque Registry/chunk payload、production trigger未実装、chat auth/outbound attribution未完了を維持する。これらを pure test や実TCP passから昇格しない。
7. 残must-fixの優先順は、(a) Fの generated item required-feature metadataと実Player/packet fixture、(b) Gの signed command/outbound secure attribution/runtime lifecycle、(c) Eの authorized vanilla-client evidence、である。いずれも今回の文書commitに混ぜない。

## 未確認事項

- この回の Rust compile/test/clippy/build結果。
- 現在の live sourceをRust 1.97.1で再コンパイルした結果。
- E test binaryの今回再実行結果。
- Fの実packet-side effect、item feature gating、vanilla client item semantics。
- Gの実duplicate-login runtime ordering、dimension transfer runtime、official chat trace。
- 正式 Java 26.2 vanilla clientの接続・render・inventory/chat acceptance。
- standalone Pumpkin serverの graceful exit code と外部 process lifecycle。

## Superseding verification record

This handoff was read before the egress/signable review. Its historical
"not run" statements describe the earlier resume task, not the current gate.
The current results are recorded in `26.2-reconfiguration-egress-fix.md`,
`26.2-signable-arguments.md`, and the development checkpoint: dedicated Rust
1.97.1 checks passed, `pumpkin-command` passed **85 unit + 5 doctests**, Python
compatibility suites passed **28/28** and **16/16**, and three consecutive
current-source real TCP runs passed. Official vanilla-client compatibility,
signed-command network dispatch, and the unrelated F/G runtime boundaries
remain unverified.

# 26.2 development checkpoint: safe local commit split

- Target: Java Edition `26.2` / protocol `776` / world data version `4903`
- Branch at checkpoint: `master` (ahead of `origin/master` by ten local commits after this evidence-record commit)
- Purpose: record the safe commit boundary for the current Luna development output. This is **not** a full compatibility approval.

## Current verification checkpoint

The independent review and integration gate completed. The CFG-T3 source/protocol/doc group was committed as `ba266a5e3`; this harness/fixture/doc group completes the planned split.

- **INV-T1:** material slots `1/2/8` are covered; a nine-occupied-slot map-only input and filled-map/filled-map input are rejected; a stacked map in one slot contributes one result and consumes one item; map component id `42` is preserved.
- **INV-T4:** the decoded close `window_id` is compared with the active handler `sync_id`; the exact close test passed with **1/1**.
- **CFG-T1:** `Accepted` and `Downloaded` remain pending; `DownloadSuccess` and other terminal statuses advance; required decline keeps its explicit kick policy; UUID mismatch remains rejected.
- **CFG-T2:** exact ordered Known Packs is distinguished from subset, empty, unknown, reordered, and duplicate responses. Both exact and fallback selections reach the real registry-data serializer, and the current generated-data limitation intentionally sends the complete registry entries rather than rejecting a legal client or omitting unowned entries.
- **CFG-T3:** the guarded `CStartConfiguration` sender, empty Play acknowledgment guard, Configuration dispatcher, shared Known Packs/Registry Data/Tags/Finish path, config keepalive routing, and exactly-once Finish-to-Play return are committed in `ba266a5e3`. The review also added trailing-body rejection coverage. Vanilla-client and triggered reconfiguration runtime acceptance remain unverified.
- Source changes were limited to the Java reconfiguration path, the Configuration keepalive codec, and tests/docs. No generated data, manifest, dependency, server, world, binary, JAR, raw official data, `.pi/`, or goal file was changed.

Current gate evidence, all with Rust `1.97.1`, `C:/Temp/pumpkin-target-26_2-next`, `CARGO_INCREMENTAL=0`, `CARGO_PROFILE_DEV_DEBUG=0`, `CARGO_BUILD_JOBS=2`, and `--locked`:

- inventory package: **32 passed / 0 failed**, doctests **0 passed / 1 ignored**, exit `0`;
- configuration-state local tests: **4 passed / 0 failed**, exit `0`;
- pending phase tests: **3 passed / 0 failed**, exit `0`;
- reconfiguration-ack guard: **1 passed / 0 failed**, exit `0`;
- close-handler exact test: **1 passed / 0 failed**, exit `0`;
- `cargo check --locked -p pumpkin`: exit `0`, with no CFG-T3 source warning; only the known dependency future-incompatibility notice was emitted.
- Current CFG-T3 focused state tests: **5 passed / 0 failed**, exit `0`; Configuration keepalive codec test: **1 passed / 0 failed**, exit `0`.
- Full `cargo test --locked --lib -p pumpkin`: **271 passed / 0 failed**, exit `0`; full `pumpkin-protocol` lib tests: **122 passed / 0 failed**, exit `0`.
- New-source `cargo build --locked -p pumpkin`: exit `0`; debug binary `C:/Temp/pumpkin-target-26_2-next/debug/pumpkin.exe`, size **233,593,856 bytes**, SHA-256 `c9101d43c409a0b303a9ea60c8b3351338971c0f84eeddcf92edd6b799878b43`.

### Next 70-turn task units

1. **Completed commit/audit unit:** `60695903d fix(inventory): align 26.2 crafting and close guards`; cached names/stat/check/full diff were reviewed. `.pi/`, goals, runtime/data, binaries, JARs, worlds, logs, and raw official data were not staged.
2. **Completed commit/audit unit:** `89d47265b fix(protocol): harden 26.2 configuration gates`; initial-login CFG-T1/CFG-T2 sources and evidence docs were reviewed and committed.
3. **Completed CFG-T3 unit:** `ba266a5e3 feat(protocol): add 26.2 play reconfiguration path`; staged names/stat/check/full diff were reviewed. `docs/compatibility/26.2-reconfiguration.md` records the exact scope and limits.
4. **Completed evidence/doc unit:** harness/fixture additions and the prior-binary loopback record are kept separate from CFG-T3; the fresh initial-login loopback is not promoted to reconfiguration or vanilla-client approval.
5. **Inventory breadth unit:** test recipe-specific remainder behavior, cursor/full/partial drop conservation, creative boundaries, special recipes, and vanilla acceptance separately from INV-T1/INV-T4.
- Initial repository state: no staged paths; tracked source changes plus untracked `.pi/`, `docs/`, and `tools/` were present.
- Protected user-owned state: `.pi/` and `.pi/goals/` (including goal files) were not staged, edited, reset, cleaned, or committed.
- Git identity: `yuzumican <83267351+yuyu1815@users.noreply.github.com>`; no global identity or hook bypass was needed.

## Committed responsibility groups

The groups below were staged with explicit path lists. For every commit, `git diff --cached --stat`, `git diff --cached --check`, the cached name list, and the complete cached diff were reviewed before commit.

1. `97092e4c8` — `fix(protocol): harden 26.2 packet boundaries`
   - `crates/pumpkin-protocol/src/java/packet_decoder.rs`
   - `crates/pumpkin-protocol/src/java/server/play/chat_command_signed.rs`
   - `crates/pumpkin-protocol/src/java/server/play/edit_book.rs`
   - `crates/pumpkin-protocol/src/java/server/play/set_game_rule.rs`
   - Scope: inclusive compression threshold, premature uncompressed EOF rejection, and allocation-before-count checks with protocol unit tests.

2. `28bd1823d` — `fix(world): terminate corrupt chunk loads safely`
   - `crates/pumpkin-world/src/chunk/io/file_manager.rs`
   - `crates/pumpkin-world/src/chunk_system/schedule.rs`
   - `crates/pumpkin-world/src/chunk_system/tests.rs`
   - `crates/pumpkin-world/src/chunk_system/worker_logic.rs`
   - `crates/pumpkin-world/src/world_info/anvil.rs`
   - Scope: terminal corruption/load-failure handling, transitive DAG cancellation, one terminal outcome per requested coordinate, Anvil `level.dat` directory/replacement handling, and regression/integration tests.

3. `427c10a7f` — `feat(chat): add stateless 26.2 signature helper`
   - `crates/pumpkin/src/net/chat/signature.rs`
   - `crates/pumpkin/src/net/chat/mod.rs`
   - `crates/pumpkin/src/net/java/play/chat_message.rs`
   - Scope: canonical 26.2 signed-chat bytes and RSA/SHA-256 known-answer verification helper. It intentionally does not own inbound chain state, last-seen reconstruction, or acceptance policy.

4. `fbc741f1b` — `fix(player): synchronize Java hand-swap inventory slots`
   - `crates/pumpkin/src/entity/player.rs`
   - Scope: explicit selected-hotbar/off-hand `CSetPlayerInventory` synchronization plus local mapping/backing-inventory tests. Real vanilla-client UI acceptance remains unverified.

The compatibility documentation and Python tools are a separate evidence/documentation group, to be committed after this checkpoint is written. Ignored runtime artifacts are excluded.

## Intentionally uncommitted residual source

The following source remains in the working tree because the full review identified concrete must-fix or incomplete behavior. It must not be described as fully verified compatibility:

### Inventory / player crafting

- `crates/pumpkin-inventory/src/crafting/crafting_screen_handler.rs`
- `crates/pumpkin-inventory/src/player/player_screen_handler.rs`

`docs/compatibility/26.2-config-inventory-review.md` identifies:

- **INV-T1 (P1):** transmute currently restricts occupied slots to two and uses a material stack count for map cloning; official 26.2 semantics count material slots with bounds `1..=8`. Required follow-up cases are material counts `1, 2, 8`, stacked material in one slot, and invalid count `9`.
- **INV-T2/INV-T3:** remainder handling and tests cover only a narrow cake/normal-recipe path; generic recipe remainder, component, special/dynamic recipe, full/partial drop, and creative behavior remain unproved.
- **INV-T4 (adjacent P1):** `SCloseContainer.window_id` is decoded but ignored, so stale close packets can close the wrong current handler. This is not included in the inventory commit.

The current package test pass is therefore a narrow regression result, not an inventory compatibility approval.

### Configuration / login state

No CFG-T3 source remains uncommitted after `ba266a5e3`. The remaining compatibility
limits are:

- **CFG-T1:** `Accepted`/`Downloaded` resource-pack statuses remain pending until
  `DownloadSuccess`; runtime resource-pack download and vanilla acceptance remain
  unverified.
- **CFG-T2:** generated registry data has no per-entry Known Pack ownership metadata,
  so legal fallback selections intentionally serialize the complete registry payload.
- **CFG-T3:** no production caller or user command triggers reconfiguration; the
  stateful socket path and vanilla-client Play -> Configuration -> Play acceptance
  remain unverified.
- **CFG-T4:** duplicate JavaClient/Pending resource-pack policy remains to be
  unified before any future reconfiguration resource-pack offer path is added.

## Verification evidence

Fresh commands run after collecting the current final working tree, all with:

```text
RUSTUP_TOOLCHAIN=1.97.1
CARGO_TARGET_DIR=C:/Temp/pumpkin-target-26_2-next
CARGO_INCREMENTAL=0
CARGO_PROFILE_DEV_DEBUG=0
CARGO_BUILD_JOBS=2
--locked
```

- `py -m unittest discover -s tools/compat-data -p 'test_*.py' -v`: exit `0`, **16 passed**.
- `py -m unittest discover -s tools/compat-26_2 -p 'test_*.py' -v`: exit `0`, **22 passed / 0 failed** in the current gate.
- `py -m py_compile ...`: exit `0` for compat-data and compat-26_2 Python modules.
- `cargo test --locked -p pumpkin-protocol -- --nocapture`: exit `0`, **121 passed / 0 failed**, doctests `0/0`.
- `cargo test --locked -p pumpkin-world -- --nocapture`: exit `0`, **231 passed / 0 failed**, doctests `0 passed / 1 ignored`. This includes the real Anvil worker/scheduler fixtures and terminal cancellation tests.
- `cargo test --locked -p pumpkin-inventory -- --nocapture`: exit `0`, **32 passed / 0 failed**, doctests `0 passed / 1 ignored`. Scope remains limited as stated above.
- `cargo test --locked --lib -p pumpkin net::chat::signature::tests -- --nocapture`: exit `0`, **11 passed / 0 failed**.
- `cargo test --locked --lib -p pumpkin entity::player::tests -- --nocapture`: exit `0`, **6 passed / 0 failed**.
- `cargo check --locked -p pumpkin`: exit `0`; no CFG-T3 source warning, only the known dependency future-incompatibility notice for `proc-macro-error2 v2.0.1`.
- Current CFG-T3 focused state test: **5 passed / 0 failed**; Configuration keepalive codec test: **1 passed / 0 failed**, **121 filtered out**.
- Current full `cargo test --locked --lib -p pumpkin`: **271 passed / 0 failed**; current `cargo check --locked -p pumpkin-protocol`: exit `0`.
- `rustup run 1.97.1 rustfmt --check --edition 2024 <all modified Rust files>`: exit `0`.
- `git diff --check`: exit `0`; Git emitted only existing LF-to-CRLF conversion warnings.

The package tests, check, build, and fresh source-binary initial-login loopback were local gates. The loopback used `127.0.0.1:25577`, status/login harness exits `0`, and strict result `first_playable_strict`; it did not trigger reconfiguration. Server stdin `stop\n` produced exit `3221225725` (`0xC00000FD`, stack overflow), so graceful lifecycle exit `0` is not claimed. No vanilla client, reset/clean/amend/rebase/force/push, or hook bypass was performed.

## Compatibility limits and uncommitted evidence

- The synthetic protocol harness remains separate from vanilla-client evidence. Strict-play, opaque payload, empty Known Packs, and loopback results do not prove registry/chunk semantics or official client acceptance.
- Chat helper tests prove canonical serialization and RSA verification only. Inbound chain index/timestamp state, atomic last-seen update/checksum, session expiry integration, signed-command verification, and secure broadcast policy remain incomplete.
- Configuration loopback normal/negative observations in `26.2-postcommit-loopback.md` use the old source `89d47265b` binary and are not CFG-T3 runtime evidence. The fresh source-binary initial-login loopback also ended with Windows exit `3221225725` (`0xC00000FD`, stack overflow), so graceful lifecycle exit-0 is not claimed.
- Existing official JAR/client artifacts, world/log/binary output, Python cache, and `tools/compat-data/.cache/` raw upstream data were not staged. `tools/compat-data/LICENSE-minecraft-data.txt` and `THIRD_PARTY_NOTICES.md` were reviewed; cache files are ignored and remain outside the commit.
- C: free space after tests was `215,840,317,440` bytes, above the `30 GiB` stop threshold. No repository world/log/binary/JAR was added.

## Next required fixes

1. Fix `INV-T1` using official material-slot-count semantics and add the `1/2/8/9` differential tests.
2. Fix `INV-T4` by checking `SCloseContainer.window_id` against the active handler before close/resync.
3. Fix `CFG-T1` Downloaded/DownloadSuccess state handling and unify Pending/JavaClient resource-pack policy.
4. Add a server-owned test seam for `start_reconfiguration`, then run stateful TCP and legal/vanilla-client acceptance evidence without promoting pure tests or the initial-login loopback to full compatibility.
5. Add full/partial remainder/drop and vanilla-client evidence without promoting synthetic or pure helper results to full compatibility.

## Entity persistence review checkpoint (current task)

The uncommitted entity persistence group was independently reviewed and tightened before commit. The responsibility scope is the eight original tracked persistence files plus the required `pumpkin-world` shutdown-test callsite update and the two persistence review documents; `.pi/` remains protected and untracked.

Resolved P1 paths:

- `Level::entity_save_lock` now covers normal entity load insertion, save load/UUID merge/write, generation publication protection, eviction map removal/write, and final shutdown cache flush. The reviewed order is Level save lock -> serializer lock -> watcher access; no reverse lock cycle was found.
- `Loaded`, `Missing`, and `Error` are separated. Save paths never use generation fallback for a read error; normal entity loading logs the error and does not replace corrupt bytes with an empty generated chunk.
- Missing-save snapshots are inserted with UUID replacement semantics and generation uses `entry().or_insert`, preventing an empty generation from replacing a newer saved entity. Eviction save/cleanup failures retain the entity or cache for retry and stop cleanup in every checked caller.
- `World::save_entity` uses serialized `Pos` for the destination chunk, and the boxed `tokio::spawn` task propagates both `JoinError` and serializer errors. World/Server/Level/WASM save callers now propagate `Result` rather than claiming a successful save.

Fresh current-source evidence with Rust `1.97.1`, target `C:/Temp/pumpkin-target-26_2-next`, incremental/debug disabled, jobs `2`, and `--locked`:

- `cargo fmt --all -- --check`: exit `0`.
- `cargo check --locked -p pumpkin-world`: exit `0`.
- `cargo check --locked -p pumpkin`: exit `0`.
- `cargo test --locked -p pumpkin-world -- --nocapture`: **234 passed**, 0 failed, 1 doctest ignored, exit `0`.
- `cargo test --locked --lib -p pumpkin -- --nocapture`: **271 passed**, 0 failed, exit `0`.
- Focused entity IO tests: **2 passed** for UUID/type/marker reopen and malformed raw bytes unchanged; **1 passed** for generation not replacing a saved snapshot. Anvil failure fixtures: **2 passed**.
- `cargo build --locked -p pumpkin --message-format=short`: exit `0`; debug binary **233,670,656 bytes**, SHA-256 `4c7c0ac0a29ccf001ce6984d7c41c025b4c79efab3445619c6481fe9a3cd7943`.
- Isolated current-binary localhost start/`stop`: exit `0`; `Completed save!` and `The server has stopped.`; no save failure, stack overflow, or `Skipping entity` output. The run had no connected entity client.

The full review is in `docs/compatibility/26.2-persistence-final-review.md`. It does not claim the unimplemented concrete World spawn -> boundary move -> second-process reopen case or a global entity snapshot/version freeze. Original repository `target/` measured approximately **19G** and was not deleted; only the dedicated **8.0G** target was used for this task. C: free space was approximately **185.46 GiB**. Do not stage `.pi/`, goals, worlds, logs, cache, binaries, JARs, raw official data, or generated runtime output.

## Integrated independent-review response checkpoint

The AentityE2E/Battack/Cchat/Dharness review was re-read in full before this
checkpoint. Only independent verified responsibilities were committed; the
attack/interact responsibility remains intentionally unstaged because official
item-feature and `cannotAttackWithItem` guards have no complete Pumpkin-side
representation.

Committed responsibility units:

- `44b5b3319` — `fix(world): close 26.2 entity move persistence boundary`.
  `Level::save_entity_nbt_with_stale_chunk` removes a moved UUID from cached and
  supplied non-resident previous chunks; World supplies `Entity::last_pos`.
- `56943b3af` — `style(world): format entity persistence regression`.
  Formatting-only follow-up; it did not change the persistence contract.
- `57ab802ca` — `fix(chat): validate 26.2 inbound chain state transactionally`.
  Wire-millisecond ordering, sticky `ChainBroken`, idempotent same-session
  update, cloned ACK preview, and success-only commit are covered. The helper
  `clear_inbound_state` exists, but the disconnect lifecycle hook is not claimed
  until its owning world path is committed.
- `1c7e5a47d` — `test(protocol): add 26.2 reconfiguration harness evidence`.
  The TCP harness is wait-only and preparation-only; no trigger or server run is
  promoted to compatibility evidence.

Focused verification, all with Rust `1.97.1`, target
`C:/Temp/pumpkin-target-26_2-next`, incremental/debug disabled, jobs `2`, and
`--locked`:

- `cargo test --locked -p pumpkin-world level::tests::disk_only_stale_entity_snapshot_is_removed_on_boundary_save -- --nocapture`:
  exit `0`, **1 passed / 0 failed / 234 filtered**.
- `cargo test --locked --lib -p pumpkin world::tests::world_entity_persistence_e2e_spawn_move_stop_reopen -- --nocapture`:
  exit `0`, **1 passed / 0 failed / 282 filtered**.
- `cargo test --locked --lib -p pumpkin net::chat::state::tests -- --nocapture`:
  exit `0`, **6 passed / 0 failed / 277 filtered**.
- `cargo test --locked --lib -p pumpkin net::java::play::attack::tests -- --nocapture`:
  exit `0`, **5 passed / 0 failed / 278 filtered**.
- `cargo check --locked -p pumpkin`: exit `0` (known dependency future-incompatibility notice only).
- `py -m unittest discover -s tools/compat-26_2 -p 'test_*.py' -v`: exit `0`, **23 passed / 0 failed**; `py_compile` exit `0`.
- `cargo fmt --all` and `git diff --check`: exit `0` for the verified/committed
  groups (Git emitted existing LF-to-CRLF conversion warnings only).

Residuals and explicit non-claims: world-border/dragon-part/PiercingWeapon
source guards are compiled but lack runtime client/plugin-counter evidence;
item-feature and `cannotAttackWithItem` guards remain unresolved; signed
command argument verification, outbound secure attribution, independent chat
runtime fixtures, and disconnect sidecar cleanup remain unresolved. No server
start, vanilla client, TCP reconfiguration trigger, or reconfiguration result
is claimed.

Concrete reconfiguration seam: add the test-only
`crates/pumpkin/tests/java_reconfiguration_tcp.rs`, start an isolated loopback
server, establish strict Play baseline, retain the same `Arc<Player>`, invoke
`JavaClient::start_reconfiguration().await` twice after each returned Play
keep-alive, and assert empty acknowledgements, Known Packs/Finish ordering,
exactly-once finish claim, same entity/position, and EOF during Configuration.

## Continuation gate checkpoint (current worktree)

The continuation review is recorded in
`docs/compatibility/26.2-continuation-gate.md`. The ChatOwnerToken/runtime
review found that retired old owners cannot reset, verify, or clear replacement
state in the covered production path; the real fixture covers Server/World,
EntityTracker, CachedStatus, transfer, duplicate-login, delayed old work, and
32 cleanup cycles. This does not claim official session/RSA or vanilla-client
acceptance.

The reconfiguration review found one evidence-backed harness false rejection:
protocol 776's current Pumpkin path legally omits Update Enabled Features even
though the packet is mapped. The tracker now validates it when present but does
not require it. `py -3 -m unittest discover -s tools/compat-26_2 -p 'test_*.py' -v`
passes **28/28** and `py_compile` passes; cargo focused state/signature tests
pass (**13/13** and **11/11**) and `cargo check -p pumpkin` exits `0`. Three
final current-source real-TCP attempts were executed and all exited `101`; the
normal path reached Configuration cycle 1 in one attempt but the repeated gate
still hit mapped Play collisions (`83`/`54`) in cycle 2 or failed diagnostic
parsing. The reconfiguration production barrier and integration test remain
uncommitted. The earlier `pending_bytes=24, closed=true` observation remains a
flake observation, not a proven root cause. No official client run or
signed-command implementation is claimed.

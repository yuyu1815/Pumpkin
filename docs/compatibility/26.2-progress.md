# Minecraft Java 26.2 compatibility progress

- 対象: Java Edition 26.2 / protocol `776` / world data version `4903`
- 確認時点: 2026-09-19、再開ラウンドの独立レビュー後
- HEAD: `1ac447f2918b7dec7d19a5836fa3673d4fe3c9f5`
- 判定: **実 Pumpkin に対する synthetic strict-play の第一到達点は確認済み。ただし vanilla 26.2 公式 client 未検証であり、全 gameplay 完全互換も未達。**
- 操作制約: `git reset`、`git clean`、commit は未実行。既存のユーザー `.pi/` は保持し、編集していない。

## Findings

### 今回確認できた達成事項

1. **Protocol decoder の compression threshold 境界**
   - `crates/pumpkin-protocol/src/java/packet_decoder.rs`
   - `TCPNetworkDecoder::get_raw_packet` の未圧縮判定を `raw_packet_length > threshold` から `>=` に変更。
   - protocol の圧縮仕様上、packet data 長が threshold と等しい場合も compression 必須であることを、`java::packet_decoder::tests::reject_uncompressed_packet_at_compression_threshold` で固定。

2. **Anvil level.dat 保存**
   - `crates/pumpkin-world/src/world_info/anvil.rs`
   - `AnvilLevelInfo::write_world_info` に `create_dir_all(level_folder)` を追加し、新規 world で親ディレクトリがない場合の初回保存を可能にした。
   - `replace_level_dat` を追加し、Windows で既存 destination への rename が `AlreadyExists` になる場合は destination を削除して再 rename。その他の I/O error は握りつぶさず返す。
   - `world_info::anvil::test::rewrite_level_dat_replaces_existing_destination` で `level.dat_new` の残留なしと DataVersion `4903` の再読込みを確認。

3. **main/off-hand swap の inventory 同期**
   - `crates/pumpkin/src/entity/player.rs::Player::swap_item`
   - 選択 hotbar slot (`0..=8`) と off-hand slot (`40`) を `CSetPlayerInventory` で明示送信し、既存の equipment broadcast と `sync_inventory_to_client` を維持。
   - `hand_swap_inventory_slots`、`entity::player::tests::hand_swap_sync_covers_selected_and_off_hand_slots`、`entity::player::tests::player_inventory_swap_changes_the_two_synced_slots` を追加。
   - 現テストは slot mapping と server-side inventory の交換結果を確認する。実 Java client の UI 表示、packet受理、再接続後保存までは確認していない。

4. **strict-play harness**
   - `tools/compat-26_2/protocol776_harness.py` に `--strict-play` を追加。
   - status/login/configuration の state 遷移、Known Packs 応答、Join Game の全 envelope、Player Position と teleport confirmation、first chunk の外側構造、Keep Alive echo を同一接続で検査。
   - `assets/packet/26_2_packets.json` を runtime source として critical mapping を照合し、packet ID の新規推測はしていない。
   - `tools/compat-26_2/test_protocol776_harness.py` は 13 tests。negative test を含め全件成功。

### 互換性として主張してはいけない事項

- **vanilla 26.2 公式 client の接続は未検証。** strict harness は Minecraft client ではなく、opaque payload を含む synthetic TCP peer。
- **全 gameplay 完全互換は未達。** block place、GUI inventory click、通常移動の長時間 trace、追加 chunk の意味論、respawn、chat signing/rendering、resource pack、online-mode/encryption、Bedrock bridge 等は未検証。
- Known Packs には空リストを返しており、registry の同一性を確認した証拠ではない。
- chunk は first chunk の座標、heightmap key/長さ、section-data length、block entity count、light array の境界を検査しただけで、palette、block state、biome、light の意味論や client-side acceptance は未検証。
- strict の `teleport_confirms_sent=[1]` は server に Accept Teleportation を送った事実であり、vanilla client が画面上で受理した証明ではない。

## Relevant Files

### 正式コード変更

- `crates/pumpkin-protocol/src/java/packet_decoder.rs`
  - threshold 境界修正、回帰 test 1件。
- `crates/pumpkin-world/src/world_info/anvil.rs`
  - 初回 world directory 作成、Windows-safe な `level.dat` replacement、回帰 test 1件。
- `crates/pumpkin/src/entity/player.rs`
  - hand swap の selected/off-hand slot 同期、helper、回帰 test 2件。

### 検証コード・記録

- `tools/compat-26_2/protocol776_harness.py`
  - `verify_critical_mapping`、`parse_join_game`、`parse_player_position`、`parse_first_chunk`、`StrictPlayTracker`、`run_strict_play`。
- `tools/compat-26_2/test_protocol776_harness.py`
  - 13 Python unittest。
- `docs/compatibility/26.2-play-validation.md`
  - strict 実行の詳細と限界。
- `docs/compatibility/26.2-gameplay-audit.md`
  - gameplay 読解、hand swap 修正、未検証範囲。
- `docs/compatibility/26.2-integration.md`
  - world/protocol package test と先行実 Pumpkin smoke の記録。

`docs/compatibility/` と `tools/compat-26_2/` は現時点で未追跡、上記3つの Rust source は tracked diff。`assets/packet/26_2_packets.json` は変更なし。

## Migration / Investigation Notes

### 実行した確認コマンドと結果

#### Harness

```text
py -m unittest discover -s tools/compat-26_2 -p "test_*.py" -v
```

- exit `0`
- `Ran 13 tests in 0.005s`
- `OK`

```text
py -m py_compile tools/compat-26_2/protocol776_harness.py tools/compat-26_2/test_protocol776_harness.py
```

- exit `0`

#### 現ツリーの関係する Rust 局所 test

全コマンドで次を設定した。

```text
RUSTUP_TOOLCHAIN=1.97.1
CARGO_TARGET_DIR=C:/Temp/pumpkin-target-26_2-next
CARGO_INCREMENTAL=0
CARGO_PROFILE_DEV_DEBUG=0
CARGO_BUILD_JOBS=2
--locked
```

```text
cargo test --locked -p pumpkin-protocol java::packet_decoder::tests::reject_uncompressed_packet_at_compression_threshold -- --exact --nocapture
```

- exit `0`
- `1 passed, 0 failed, 109 filtered out`

```text
cargo test --locked -p pumpkin-world world_info::anvil::test::rewrite_level_dat_replaces_existing_destination -- --exact --nocapture
```

- exit `0`
- `1 passed, 0 failed, 224 filtered out`

```text
cargo test --locked --lib -p pumpkin entity::player::tests -- --nocapture
```

- exit `0`
- `6 passed, 0 failed, 244 filtered out`
- 追加 hand-swap test 2件を含む。

```text
rustfmt --check --edition 2024 \
  crates/pumpkin-protocol/src/java/packet_decoder.rs \
  crates/pumpkin-world/src/world_info/anvil.rs \
  crates/pumpkin/src/entity/player.rs
```

- exit `0`

```text
git diff --check
```

- exit `0`

先行統合記録では、同じ専用 target で `cargo test --locked -p pumpkin-world` が `225 passed, 0 failed`、`cargo test --locked -p pumpkin-protocol` が `110 passed, 0 failed`、`cargo build --locked -p pumpkin` が exit `0`。今回のレビューでは重い package 全体 test/build を無理由に再実行せず、現 gameplay source の compile を含む player test と変更点の exact test を再実行した。

### strict-play 実 Pumpkin の観測

実験パス:

- binary: `C:/Temp/pumpkin-target-26_2-next/debug/pumpkin.exe`
- binary size: `231,161,856` bytes
- binary SHA-256: `7CFE4E1B9D2012D7E8E6E67BD354AC34553628D7183A9F91126ECC5512D639A2`
- run/world: `C:/Temp/pumpkin-26_2-strict-validation`
- evidence: `C:/Temp/pumpkin-26_2-strict-validation/evidence`
- bind: `127.0.0.1:25568` only
- `online_mode=false`、`encryption=false`、Bedrock disabled、telemetry disabled

```text
py tools/compat-26_2/protocol776_harness.py --mode status --host 127.0.0.1 --port 25568 --timeout 5
```

- exit `0`
- `protocol=776`
- `status_pong=pass`
- `status_version.name=1.7.2-26.2`
- evidence: `status.json`、119 bytes

```text
py tools/compat-26_2/protocol776_harness.py --mode login --strict-play --host 127.0.0.1 --port 25568 --timeout 40
```

- exit `0`
- `compression_threshold=256`
- `login_packets=2`
- `configuration_packets=35`
- `known_pack_reply=true`
- `play_packets=7961`
- `opaque_play_packets=7957`
- Join Game: entity `1`、dimensions `overworld/nether/end`、dimension `minecraft:overworld`、sea level `63`
- `position_syncs=1`
- `teleport_confirms_sent=[1]`
- first chunk: `(0,0)`、heightmaps `[1,4,5]`、section data `22921` bytes、block entities `0`、sky arrays `16`、block arrays `6`
- `keepalives_replied=1`
- `strict_play_validation=pass`
- `result=first_playable_strict`
- `real_client_compatibility=not_claimed`
- evidence: `strict-play.json`、1321 bytes

strict run の server log (`C:/Temp/pumpkin-26_2-strict-validation/evidence/server.stdout.log`) は `Server is now running`、`compat_26_2 joined the game`、`compat_26_2 left the game`、`Completed save!`、`The server has stopped.` を含む。stdin に `stop\n` を送り、server exit code `0`。force kill ではない。レビュー時の再確認でも `PUMPKIN_PROCESS_PRESENT=0`、port `25568` の listen `0`。

注意: binary の更新時刻は 01:49:57、`player.rs` の今回の hand-swap source 更新時刻は 02:03:48 であり、この strict binary は hand-swap 修正後 binary ではない。strict は hand-swap を操作しないため、変更後 binary の再build/runは行わなかった。hand-swap については上記の現ツリー compile/test を証拠とし、実 client acceptance は未確認とする。

### 容量・成果物・変更保護

レビュー時の実測:

- C: free `243,331,858,432` bytes
- `C:/Temp/pumpkin-target-26_2-next` recursive file bytes `8,777,116,682`
- cargo/build による free の増減は他 process の影響を含むため、使用量差分とは解釈しない。

リポジトリ内の JAR/binary/world/log の誤追加を確認するため、repo の `.git` と `.pi` を除いて `*.jar`、`*.exe`、`*.bin`、`*.zip`、docs/tools 配下の `*.log`、`level.dat`、`*.mca` 等を走査したが、該当出力は `0` 件。`git status --short --untracked-files=all` でもこれらの実験成果物は確認されない。実験 binary/world/log は `C:/Temp` のみ。

`.pi/goals/.goals-pool-snapshot.json` と `.pi/goals/archived/goal_2026091900272524_mu743xgn-16cwoy.md` は既存の未追跡ユーザー領域として保持した。削除、reset、clean、commit はしていない。

## Recommended Next Steps

1. Java 25 と利用権限/EULA が準備できた別環境で、vanilla 26.2 client を loopback に接続し、client log、Join Game/chunk 受理、画面表示を synthetic harness と分離して取得する。
2. first chunk の section palette、biome、block state、light、非空 block entity NBT を意味論まで検査し、公式 26.2 registry/known-packs と payload 比較する。JAR/world/log は repo 外に置く。
3. 実 client で F-key hand swap、hotbar/off-hand UI、held equipment、再接続後状態を確認する。現状の2 testは client network acceptance を証明しない。
4. block place、GUI inventory click、通常移動・追加 chunk・respawn・chat signing・online-mode/encryption を小さな実験単位で追加する。
5. 次回も重い全 workspace build/test を先に繰り返さず、変更 package の局所 test、容量、process、repo artifact scan を先に確認する。

## 26.2 fix round follow-up: world corruption review resolution

- 独立レビュー F-1 の追加修正を実装した。`GenerationSchedule::terminal_cancel_nodes` が load-failure root から非 in-flight descendant を terminal cancellation し、通常の `drop_node` の successor queue 解放を行わない。Surface→Carvers（read radius 0）→後続 stage の誤再実行を regression test で固定した。
- `ChunkFileManager::fetch_chunks` は batch 内全座標へ Missing/Error の terminal outcome を返し、`io_read_work` の unexpected channel close は Missing ではなく `RecvChunk::LoadFailure` として終端する。failure holder は `chunk=None` のままで save path に入らない。
- fresh validation: `pumpkin-world` **229 passed / 0 failed**、`pumpkin-protocol` **121 passed / 0 failed**、`pumpkin-inventory` **30 passed / 0 failed**、`cargo check --locked -p pumpkin` exit `0`。新規 world cancellation 2件と worker classification 2件を含む。
- validation record: `docs/compatibility/26.2-fix-round-validation.md`。ログは `C:/Temp/pumpkin-validation-26_2/`、target は `C:/Temp/pumpkin-target-26_2-next`。toolchain `1.97.1`、incremental `0`、debug `0`、jobs `2`、全 cargo `--locked`。
- inventory の反証範囲は oak log→oak planks の通常 shaped recipe、2x2/3x3、partial/empty/full/repeated QuickMove に限定する。remainder/component/special/dynamic/recipe-book/実 client/drop lifecycle は未確定。retry backoff/quarantine と実 Anvil corruption fixture も残タスクであり、完全修正とは主張しない。

## 2026-09-20 compatibility loop: piston persistence

- 実行時 HEAD: `804c1e145fd4e2cd470bbb2163a0fe7785a31440`、branch `master` は `origin/master` より `ahead 3`。開始時からの既存差分は `docs/compatibility/26.2-vanilla-client-validation.md` の4行と untracked `crates/pumpkin/tests/__pycache__/protocol776_reconfiguration_client.cpython-311.pyc`。これらは保持し、本ラウンドでは commit/push/reset/clean を実施していない。
- 候補調査: BLK-01/BE-01/TICK-01/TICK-02 は既存監査の静的高確度残件、TooltipDisplay の codec loss と Adventure predicate evaluator は既存 checkpoint の未完、PST-01 は `PistonBlockEntity::from_nbt` の固定 AIR と `write_nbt`/`chunk_data_nbt` の TODO による保存損失を入力→復元→完了経路で確認できた。PST-01 の最小再現は moving piston の公式 NBTを `from_nbt` へ渡し、完了時の `pushed_block_state` を確認すること。
- 公式成果物の根拠: read-only `C:/Temp/minecraft-26_2-inner.jar`（SHA-256 `183c0499c5f855570ee487dd38e141a53f0121f83a0b07a3bac2d8b6698823e8`）の `net/minecraft/world/level/block/piston/PistonMovingBlockEntity.class` constant pool に `blockState`、`movedState`、`progress`、`facing`、`extending`、`source` が存在することを確認した。併せて `C:/Temp/minecraft-26_2-server.jar` の `version.json` は `id/name=26.2`、`protocol_version=776`、`world_version=4903`、`java_version=25`（SHA-1 `823e2250d24b3ddac457a60c92a6a941943fcd6a`）だった。`NbtUtils.class` の独立 static inspection でも `readBlockState`/`writeBlockState`、`Name`、`Properties`、`writeStateProperties` を確認したが、手元環境に `javap` がなく invocation bytecode は未確認。したがってこれは official 26.2 artifact の version/key/codec-symbol evidence であり、vanilla runtime実行、全Property意味論、client受入の証明ではない。
- 修正: `crates/pumpkin/src/block/entities/piston.rs` に `blockState` の `Name`/`Properties` codecを追加し、`from_nbt`、`write_nbt`、`chunk_data_nbt` の全経路で `pushed_block_state` を保持する。Propertiesなしは対象blockのdefault state、未知block・未知property/value・Properties/childの不正型・解決不能stateは明示的に AIR fallback とした。stoneに加え `minecraft:end_rod` の非default `facing=east` fixtureで両保存経路を復元確認し、invalid fallback回帰testも追加した。
- red/greenの境界: 修正前の既存stone回帰testは `cargo test --locked -p pumpkin --lib block::entities::piston::tests::piston_from_nbt_restores_official_pushed_block_state -- --nocapture` exit `101`、`0 passed / 1 failed / 309 filtered out`（これは追加assert/testのredではない）。追加したproperties/fallback assertの修正前redは未測定。修正後はpiston 3 testsを各 exit `0`、各 `1 passed / 311 filtered out`、`cargo test --locked -p pumpkin --lib -- --nocapture` exit `0`、`312 passed`。
- 追加確認: `cargo fmt --all -- --check` exit `0`、`git diff --check` exit `0`、`cargo check --locked -p pumpkin` exit `0`（future-incompat warning 1件）。最終共通検証は `py -3 -m unittest discover -s tools/compat-26_2 -p 'test_*.py' -q` exit `0`（28 tests）、`py -3 -m unittest discover -s tools/compat-data -p 'test_*.py' -q` exit `0`（16 tests）、`py -3 tools/compat-data/compat_data.py verify` exit `0`、`cargo test --locked -p pumpkin --test java_reconfiguration_tcp -- --nocapture` exit `0`、`1 passed / 0 failed`（synthetic peer real TCP）。
- 未確認: pistonの実Anvil save→chunk unload→reload→tick完了E2E、公式 vanilla client、Properties全blockの公式runtime意味論、`javap` bytecode invocation、TooltipDisplay loss、Adventure evaluator、BE pending lifecycle、全workspace CI/clippy。TCP/harness成功はPiston仕様一致やvanilla client compatibilityを意味しない。

## 2026-09-20 integration review and final verification

- 保全対象を含む開始差分を再確認した。HEAD は `804c1e145fd4e2cd470bbb2163a0fe7785a31440`、`master...origin/master [ahead 3]`。tracked diff は今回の `basic.rs`、protocol `data_component.rs`、`piston.rs`、world `mod.rs` と既存 progress/vanilla-validation docs の6ファイル（`462 insertions(+), 20 deletions(-)`）。ユーザー差分の `docs/compatibility/26.2-vanilla-client-validation.md` 4行と untracked `crates/pumpkin/tests/__pycache__/protocol776_reconfiguration_client.cpython-311.pyc` は保持した。cached diff は空、`Cargo.lock` diff はなく、commit/push/reset/clean は未実施。
- TooltipDisplay は `hide_tooltip: bool` と `Vec<DataComponent>`、NBT の `hide_tooltip`（省略時 false）/`hidden_components`（省略時空集合）、wire の bool→VarInt count→registry ID を全 caller、`read_data`/`write_data`、generated ID/name mapping、DEFAULT互換値、Adventure predicate の特別分岐まで追跡した。公式 `/tmp/server-26.2.jar` は **24,952,681 bytes / SHA-256 `183c0499c5f855570ee487dd38e141a53f0121f83a0b07a3bac2d8b6698823e8`**、`version.json` は `id=26.2`, `protocol_version=776`, `world_version=4903`, `java_version=25`。`'/c/Program Files/Java/jdk-21/bin/javap.exe'` exit `0` で公式 `TooltipDisplay` の record fields、NBT key、`DEFAULT`、bool+collection `STREAM_CODEC`、`SequencedSet` による重複排除を確認し、`ByteBufCodecs.collection` の既定 max は `2147483647` と確認した。
- Tooltip の未知 ID、負数 count、既知 ID の重複はそれぞれ reject/reject/set semantics。`MAX_TOOLTIP_HIDDEN_COMPONENTS=256` は既存の protocol collection 防御上限に合わせた実装上の上限で、公式 wire の明示上限ではない。ただし現 `assets/data_component.json` の distinct component は111件なので、通常の vanilla set は拒否しない。重複を257件以上含む非canonical wire は公式 generic collection より狭くなるため、将来の互換性レビュー対象として残す。`cargo run --locked --manifest-path tools/pumpkin-codegen/Cargo.toml -- data_component` は exit `0`、generated diff は0行で不要差分なし。
- Tooltip の前担当回帰は旧 `write_data` が `NbtTag::End` のままで失敗した red、修正後 green 1件。今回の再検証は `cargo test --locked -p pumpkin-data` **66 passed / 0 failed**、`cargo test --locked -p pumpkin-protocol` **132 passed / 0 failed**（Tooltip/Adventureを含む）。
- world `remove_block_entity` の全 caller（`set_block_state`、piston の finish/tick、fill）を確認した。live の chunk-index と pending NBT を同一座標で削除し、どちらかを削除した時だけ custom data を消し、chunk を dirty にする。DashMap mutable guard は closure 終了後に chunk lock を取得し、await 中に guard を保持しないため、確認範囲で lock-order/deadlock は見つからない。新 test は旧実装で stale pending assert が red、修正後 green 1件。TempDir、loaded chunk、`ChunkData::to_bytes/from_bytes` を使うが、実 Anvil save→unload→reload E2E ではない。air→furnace の元実装挙動を実測済みとは主張せず、air 置換時の stale pending NBT 再現範囲だけを記録する。
- 実行結果: `py -3 -m unittest discover -s tools/compat-26_2 -p "test_*.py" -v` exit `0`（28 tests）、同 `tools/compat-data` exit `0`（16 tests）、`py -3 tools/compat-data/compat_data.py verify` exit `0`。`cargo test --locked -p pumpkin --test java_reconfiguration_tcp -- --nocapture` exit `0`（1 passed）、`cargo test --locked -p pumpkin --lib -- --nocapture` exit `0`（313 passed / 0 failed）、`cargo test --locked -p pumpkin --lib world::tests -- --nocapture` exit `0`（9 passed / 0 failed / 304 filtered）、`cargo test --locked -p pumpkin-world` exit `0`（235 passed / 0 failed、doctest 1 ignored）、`cargo fmt --all -- --check` exit `0`、`git diff --check` exit `0`。
- CI の `.github/workflows/rust.yml` を確認後、最後に `cargo clippy --locked -p pumpkin --all-targets --all-features -- -D warnings` を一度実行したが exit `101`。失敗は今回変更箇所ではなく既存 `crates/pumpkin-data/src/data_component_impl/combat.rs:194` の `AttributeModifierSlot::Any => unreachable!()` に対する `clippy::unreachable`。workspace nextest/doctest、全 workspace Clippy/release、Pistonの実Anvil E2E、vanilla client は未実行。
- 環境: shell は `/usr/bin/bash`、`COMSPEC=C:\WINDOWS\system32\cmd.exe`。途中の初期 build/旧 fixture `get_or_fetch_chunk` 待ち timeout は既往記録どおりで、今回の最終個別 cargo rerun は完走した。最初の結合 cargo 出力は harness の出力上限を超えたため、cargo/rustc の残存を `tasklist` で確認して終了を待ち、最終 rerun のログで exit を採取した。自分の cargo/rustc/test 子プロセスのみ終了確認し、他ユーザープロセスは触れていない。最終の正しい single-quote PowerShell process/listener check は exit `0`・該当プロセス/port出力なし。最初の PowerShell診断は bash の `$_` 展開で exit `1`、`wmic` は未搭載、`tasklist /fi` は MSYS の `/fi` path変換で失敗した。これらは今回の診断コマンドの既知の引用/環境問題であり、過去の CMD 謎エラーは元ログがなく、パイプ終了コード・引用符・timeout のどれが原因か断定しない。
- 未検証/次の上位3件: (1) 257件超の重複を含む Tooltip wire と公式 decoder の受入差を、上限撤去を含めて判断する、(2) block entity の実 Anvil save→chunk unload→reload→replacement E2E、(3) official vanilla 26.2 client の接続・chunk受入・画面表示。今回の synthetic TCP/harness は vanilla client compatibility の証明ではない。

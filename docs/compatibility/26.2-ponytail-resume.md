# 26.2 再開・ponytail 適用チェックポイント

## Findings

- 対象 cwd は `C:/Users/yuzum/Desktop/mine_rust_server/Pumpkin`。現在は `master`、`HEAD=b38cb0b1cf052507db30aaa2a27ea1d45e92f6b3`、`origin/master` より **15 commit ahead**。
- `git status --short --branch` の確認時点で staged は **0 paths**、tracked の unstaged diff も **0 paths**。未追跡は次の既存ユーザー領域だけだった。
  - `.pi/goals/.goals-pool-snapshot.json`
  - `.pi/goals/.goals-pool-snapshot.json.49236.1789815052101.tmp`
  - `crates/pumpkin/tests/__pycache__/protocol776_reconfiguration_client.cpython-311.pyc`
- `.pi/`、`.pi/goals/`、goal/cache は保護対象として未編集・未stage。`reset`、`clean`、`amend`、`rebase`、`push` は実行していない。commit も実行していない。
- 直近の責任境界は次の通り。
  - `8c20a7427b7878b36bbd0efda311a2f585a2585c` — `fix(protocol): serialize 26.2 reconfiguration egress`。単一 FIFO writer、egress gate、epoch/phase/state capture、Configuration 中の Play deferred、Finish release、strict TCP harness の unnamed-NBT cursor 修正。
  - `b38cb0b1cf052507db30aaa2a27ea1d45e92f6b3` — `feat(command): add opt-in signable argument extraction`。`pumpkin-command` の opt-in signable metadata と raw UTF-8 byte range 抽出。
- 継承する検証結果は、`docs/compatibility/26.2-development-checkpoint.md` と直近2文書に記録された **strict TCP 3/3 pass**、signable parser **85 unit + 5 doctests**、Python compat **28/28**、compat-data **16/16**、`cargo check -p pumpkin` exit `0`。この再開確認では cargo/build/server を再実行していないため、これらを新規実行結果とは扱わない。
- 未完了は明確に残る。
  1. `SChatCommandSigned` の production dispatch が未接続。現状は `SChatCommandSigned::read` 成功時に command text だけを通常 command handlerへ渡し、decode failure 時は `SChatCommand::read` へ fallback する。署名、session/RSA、chain index、last-seen/ack、permission/fallback policy の認証完成とはみなさない。
  2. bounded egress の close/backpressure stress と、queued/deferred/writer failure の bytes accounting 完全性は未検証。現在の `pending_bytes` は送信超過時に close するが、`close()` 自身は deferred queue を主に清掃し、FIFO channel 側・writer終了側の全経路を stress で固定していない。
  3. vanilla 26.2 client の起動・接続・画面表示は未実施。client JAR/Java 25 の read-only evidence はあるが、公式 server JAR、利用権/EULA、login、real client acceptance は blocker のまま。
- repository 内の ponytail 指示/skill は `git ls-files`、tracked grep、`find` で該当なし。適用した skill は外部の `C:/Users/yuzum/.pi/agent/npm/node_modules/@dietrichgebert/ponytail/skills/ponytail/SKILL.md`。原則は既存実装・stdlib 優先、理解後の最小差分、全 caller の根因修正、非自明ロジックに実行可能 test、認証・データ保存・安全性を簡略化しない、で固定する。repo 規約として `CONTRIBUTING.md` と `.editorconfig` も確認済み。`AGENTS.md` は cwd と親3階層に存在しなかった。

## Relevant Files

| path | symbol / role | 再開時の要点 |
|---|---|---|
| `docs/compatibility/26.2-development-checkpoint.md` | current checkpoint / next fixes | egress/signable の検証境界、production reconfiguration trigger、signed dispatch、vanilla acceptance 未確認を記録。 |
| `docs/compatibility/26.2-reconfiguration-egress-fix.md` | egress fix evidence | 3回の current-source TCP pass は packet boundary の synthetic/in-process evidence。backpressure stress と vanilla client approvalではない。 |
| `docs/compatibility/26.2-signable-arguments.md` | parser contract | `RequiredArgumentBuilder::signable`、`ParsingResult::signable_arguments`、raw substring、UTF-8 byte range、`Some(Vec::new())` を定義。network dispatch は未接続。 |
| `docs/compatibility/26.2-signed-command-contract.md` | official static contract | `MessageArgument` のみを primary production `SignedArgument` type として確認。signed decode fallback 禁止、argumentごとの chain link、ack/permission 分離が必要。 |
| `docs/compatibility/26.2-vanilla-client-validation.md` | vanilla gate | client `39,193,383` bytes / SHA-1 `2dc72797acbc1b63fc16a11c4ac393605f453754` と bundled Java 25 は確認済み。server JAR `60,894,273` / SHA-1 `823e2250d24b3ddac457a60c92a6a941943fcd6a`、EULA/legal、real client run は未解決。 |
| `crates/pumpkin-command/src/argument_builder.rs` | core `RequiredArgumentBuilder` | `signable: bool` と `.signable()` は実装済み。core 側の default false は維持する。 |
| `crates/pumpkin/src/command/argument_builder.rs` | Pumpkin wrapper `RequiredArgumentBuilder` | wrapper に `.signable()` forwarding がまだない。MessageArgument 相当の登録を正確に opt-in する最小 seam。 |
| `crates/pumpkin-command/src/node/dispatcher.rs` | `ParsingResult::signable_arguments` | parse 完了/error、redirect path、node identity、raw range の pure contract と6 test群がある。typed valueを署名入力へ戻さない。 |
| `crates/pumpkin/src/net/java/mod.rs` | `handle_play_packet`, `enqueue_encoded`, `start_reconfiguration`, `start_outgoing_packet_task`, `close` | signed branch は通常 command/fallback。egress は `MAX_PENDING_BYTES=64*1024*1024`、unbounded FIFO + deferred queue、writer batch、close accounting の検証対象。 |
| `crates/pumpkin-protocol/src/java/server/play/chat_command_signed.rs` | `SChatCommandSigned::read` | wire fields は保持するが、official argument signature count `8` の semantic upper boundはまだ明示されていない。 |
| `crates/pumpkin/tests/java_reconfiguration_tcp.rs` | strict TCP fixture | same player/position、2 cycles、EOF during Configuration の既存証拠。egress stress の追加 owner 候補。 |
| `tools/compat-26_2/environment_probe.py` / `test_environment_probe.py` | read-only vanilla preflight | hash/size/Java/EULA/server artifact presenceをredactedに確認する既存証拠。launcher login/tokenを読まない。 |

## Migration / Investigation Notes

### 実行した確認

- `git status --short --branch && git diff --cached --name-status && git diff --name-status && git log -12 --oneline --decorate --stat --no-renames` — exit `0`。staged/unstaged tracked diffなし、HEAD/b38 と 8c20 の直近責任範囲を確認。
- `git show --no-patch ... 8c20a7427 b38cb0b1c` と `git diff-tree --name-status` — exit `0`。両 commit の親、subject、変更 path を確認。
- `git ls-files | grep -i ponytail`、tracked `git grep`、`find` — exit `0`相当、repo-local ponytail file/textなし。
- `read` で `CONTRIBUTING.md`、`.editorconfig`、指定4 compatibility docs、vanilla checkpoint、signed/egress source seam を確認。
- process確認 — PowerShell query exit `0`。`cargo`、`rustc`、Pumpkin server、Java server/client の実行中プロセスは確認されなかった。`CPUMetricsServer` 等の無関係プロセスのみ。
- target/capacity — `C:/Temp/pumpkin-target-26_2-next` は存在し約 **7.2G**。repo `target/` の先行計測は **29,725 files / 51,532,692,257 bytes**。C: の現在 free は **158,297,354,240 bytes**。この pass は target を削除・再生成していない。
- `rustc --version` / `cargo --version` — exit `0`、Rust `1.98.1` / Cargo `1.98.1`。version確認のみで、build/testは実行していない。
- 1回の広域 grep と target/process集計コマンドは timeout になったが、必要な status、tracked ponytail検索、PowerShell process、target metadata は別コマンドで回収済み。timeoutによる repo変更はない。

### 既存実装からの最小判断

- parser core の新しい抽象化は不要。wrapper forwardingを1つ追加し、MessageArgument 相当の登録箇所だけ `.signable()` を明示する。String/greedy/selector/typed result/nameからの推測はしない。
- signed packet は既存 `handle_play_packet` の通常 command pathを再利用できるが、入口だけは専用 handler/seamへ分離し、`SChatCommand::read` fallbackを削除する。暗号・chain state未実装のまま valid/authenticated と宣言しない。少なくとも malformed signed packet、unknown/missing argument、no-signable command、permission failureを execution counter 前で止める設計境界を先に固定する。
- egress は新しい queue abstractionを増やさず、既存 `enqueue_encoded` / `pending_bytes` / `close` / writer の accountingを各経路で数える。既存の `decrement_pending_bytes` saturating helperを再利用し、超過時に packet dropを成功扱いにしない。
- vanilla は起動を代行しない。先に read-only prerequisite evidence と legal/user gateだけを回収し、official client acceptanceへ synthetic TCP結果を昇格させない。

## Recommended Next Steps

最大3責任に絞る。A/B は `net/java/mod.rs` の編集競合があるため、Aを先に固定し、Bの stress testを後段に置く。Cは source/server と独立した read-only/doc evidence なので並列実施可能。

1. **A — signed-command wrapper + MessageArgument exact opt-in + production seam（最優先）**
   - **Owner paths:** `crates/pumpkin/src/command/argument_builder.rs`、MessageArgument相当の登録箇所、`crates/pumpkin/src/net/java/mod.rs` の `SChatCommandSigned` branch、必要最小限 `crates/pumpkin/src/net/java/play/chat_command.rs`、wire boundを固定する場合のみ `crates/pumpkin-protocol/src/java/server/play/chat_command_signed.rs`。
   - **順序:** wrapper `.signable()` forwarding → exact opt-in登録 → dedicated signed handler seam → signed decode failureの通常 command fallback削除 → argument count `<=8` と malformed/no-fallback tests → auth未実装部分を明示した停止点。
   - **受入条件:** raw `SignableArgument::raw_value`、argument name mapping、`Some(Vec::new())`、permission pathを維持し、署名成功をRSA/session/chain/last-seen/ackの検証済みとは扱わない。`SChatCommandSigned::read` 失敗を `SChatCommand::read` へ流さない。
   - **Test command (次担当が実行):** `cargo test --locked -p pumpkin-command -- --nocapture`; `cargo test --locked -p pumpkin-protocol -- chat_command_signed --nocapture`; dedicated handler追加後は `cargo test --locked --lib -p pumpkin <signed-handler-filter> -- --nocapture`。この再開 passでは未実行。

2. **B — bounded egress queue close / bytes accounting stress**
   - **Owner paths:** Aの後に `crates/pumpkin/src/net/java/mod.rs`（`enqueue_encoded`、`start_outgoing_packet_task`、`close`、`MAX_PENDING_BYTES`）と `crates/pumpkin/tests/java_reconfiguration_tcp.rs`。新しい queue typeは作らない。
   - **順序:** (a) `MAX_PENDING_BYTES-1`/超過、(b) sender closed、(c) deferred Play close、(d) writer framing/write/flush failure、(e) repeated enqueue/close stress の各経路で `pending_bytes==0`、completion解放、close済みを確認。
   - **受入条件:** queued FIFO、deferred queue、in-flight batch、reject/drop、closeの各 byte が二重減算も未減算もなく、backpressure closeを正常送信扱いにしない。既存 strict TCP 3/3 passを壊さず、reconfiguration boundary evidenceとstress evidenceを分離する。
   - **Test command (次担当が実行):** `cargo test --locked --lib -p pumpkin net::tests::decrement_pending_bytes_saturating -- --nocapture` と追加する bounded/close focused filter、続けて `cargo test --locked --test java_reconfiguration_tcp -- --nocapture`。server/build禁止の今回 passでは未実行。

3. **C — vanilla client prerequisites の既存証拠回収（A/Bと独立して並列可）**
   - **Owner paths:** `tools/compat-26_2/environment_probe.py`、`tools/compat-26_2/test_environment_probe.py`、`docs/compatibility/26.2-vanilla-client-validation.md`。official launcher操作、login、EULA代理同意、server起動はowner範囲外。
   - **順序:** read-only probeの再現 → client/server hash/sizeとJava 25の差分記録 → user-confirmed legal gate待ち → fresh temp run directory/server binary hashの受領 → それでも実施できた場合だけ vanilla acceptance evidenceを別文書化。
   - **受入条件:** client `2dc727...`、Java 25、server JAR/EULA/legal blocker、未実施状態を具体的に保持し、`first_playable_strict` や in-process TCPを `real_client_compatibility` に昇格しない。
   - **Test command (次担当が実行):** `py -m unittest discover -s tools/compat-26_2 -p 'test_*.py' -v`、`py -m py_compile tools/compat-26_2/environment_probe.py tools/compat-26_2/test_environment_probe.py`。server/cargoなしで実施できる。

この文書の作成以外の書込み、cargo/build/server/commitはこの再開 passでは行っていない。
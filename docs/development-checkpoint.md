# 26.2 auth/egress development checkpoint

- Review documents were present and substantive: `docs/compatibility/26.2-auth-egress-review.md` was **merge不可**; gate document recorded 2 egress failures and missing production N2/no-signable handler coverage.
- Must-fix source changes in progress: signed commands now execute the authenticated `ParsingResult` and reject `PlayerCommandSendEvent` command mutation; signed auth is handled inline before later ACK packets, and ACK mutation shares `chat_lifecycle`; `try_kick` waits for egress completion before canceling the writer.
- Verified so far: `RUSTUP_TOOLCHAIN=1.97.1 CARGO_TARGET_DIR=C:/Temp/pumpkin-target-26_2-next CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 CARGO_BUILD_JOBS=2 cargo check --locked -p pumpkin` — exit `0` (one existing `proc-macro-error2` future-incompatibility warning).
- Gate evidence now collected: `net::chat::signature` 12, `net::chat::state` 15, `net::java::play::chat_command` 3, `net::java::egress_accounting_tests` 9, protocol signed-command 5, `pumpkin-command` 90, compat-26_2 Python 28, compat-data Python 16; all reported exit 0. `cargo check --locked -p pumpkin` and `cargo fmt --all -- --check` also exit 0; `git diff --check` exit 0.
- Source/docs remaining before commit: stage only the signed-auth/KAT paths and egress/completion paths in two separate commits, then verify cached full diff/stat and commit hashes. The nondeterministic forced-TCP-flush assertion is intentionally not claimed; official ACK-first/vanilla runtime/no-session signed compatibility remain incomplete and documented. Protected `.pi/goals/`, `__pycache__`, unknown user docs/data/binaries/JAR/world/logs remain unstaged.
